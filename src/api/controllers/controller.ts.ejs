// Libs
import { RequestHandler } from "express";
import { ServiceModule } from "simplyserveme";

import { controller } from "./controller";

export class <%= changeCase.pascalCase(name) %>Controller extends ServiceModule {
  public index(): RequestHandler {
    return controller((handler) => {
      handler.response<{ foo: string}>().json({ foo: "bar" });
    });
  }
}

const <%= changeCase.camelCase(name) %>Controller: <%= changeCase.pascalCase(name) %>Controller = <%= changeCase.pascalCase(name) %>Controller.getInstance<<%= changeCase.pascalCase(name) %>Controller>();
export default <%= changeCase.camelCase(name) %>Controller;