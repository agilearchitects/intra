// Libs
import { expect } from "chai";
import { describe, it } from "mocha";

describe("CustomerService", () => {
  it("Should be able to get customer with user related projects", async () => {
    expect(1).equal(1);
  });
});
